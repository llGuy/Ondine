#pragma once

#include "RenderStage.hpp"

namespace Ondine::Graphics {

class SceneComposition : public RenderStage {
public:
  SceneComposition() = default;
  ~SceneComposition() override = default;

private:

};

}
